// Public entry point. The barrel lives in `src/index.ts` (compiled to `dist/index.js`);
// this root file just re-exports it so in-repo code and examples can import from "../index".
export * from "./src/index";
