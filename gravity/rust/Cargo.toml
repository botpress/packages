[package]
name = "gravity"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdbscan = "0.12.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.150"

[profile.release]
opt-level = 3
lto = true
